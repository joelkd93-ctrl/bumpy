{
  "name": "bumpyapi-worker",
  "version": "2.0.0",
  "description": "Bumpy API - Cloudflare Worker with Turso",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
